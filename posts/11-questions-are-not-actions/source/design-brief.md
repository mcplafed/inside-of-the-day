Create a completely new, self-contained English LinkedIn infographic as `infographic.html` in this directory. Do not access any files outside this directory and do not reuse any existing visual.

Canvas: exact 1080×1350 px, rendered later at 3240×4050. This is Inside of the Day #11.

SOURCE TRUTH — no claims beyond this:
- A question about an error is an investigation request, not permission to mutate state.
- Bad shortcut: `Why did this happen?` → agent immediately says `Fixed.` → evidence fades → same failure returns.
- Better mechanism, in this exact order: `What happened` → `Evidence` → `Cause` → `Recurrence guard` → explicit remediation decision.
- Exception: contain active harm first, then investigate.
- Takeaway: `A fast fix cleans up the chat. A causal explanation improves the system.`

Mandatory copy:
- Header: `INSIDE OF THE DAY #11 · QUESTIONS ARE NOT ACTIONS`
- Headline: `I asked Claude why. Claude said “Fixed.” That was the problem.`
- Footer: `github.com/mcplafed/inside-of-the-day` on the left and `@vitalylobachev` on the right.

Art direction:
- Show two contrasting paths, not a generic card grid: a compact rust “fast fix” path on the left and a larger, calmer investigation-gate path on the right. The right path is the solution and must read first after the headline.
- Use warm parchment background, thin ink rules, white cards with thin grey borders, and two very pale solid semi-circles behind content: terracotta in the upper right, muted blue in the lower left. No grid, gradients, stock art, 3D, or shadows.
- Typography: one sans-serif family only (Arial/FreeSans/Helvetica) for content; monospace only for header/footer metadata and literal commands. Use a restrained type scale (four sizes maximum) and two weights maximum. Do not mix serif, extra fonts, or decorative uppercase at large sizes.
- Add at most one small inline-SVG icon if it directly supports the investigation gate. No icon set.
- Keep all decisive labels comfortably readable at a 400×500 mobile feed. Do not hide the explanation in tiny footer copy.
- Place the boundary line and takeaway above the footer.
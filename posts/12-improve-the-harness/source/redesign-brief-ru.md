Create a completely new, self-contained Russian LinkedIn infographic as `infographic.html` in this directory. Do not access any files outside this directory and do not reuse any prior design.

The user's exact headline must be the main visual headline, in Russian exactly as written:
`Не спешите зарывать длинную много часовую сессию!`

The visual must explain the subject immediately, not merely advertise a prompt.

Required logical canvas: 1080x1350. No external assets, no gradients, no background grid. Use a warm clean parchment background, dark slate ink, terracotta/orange to mark action, muted green for the final outcome. Large Arial/FreeSans-style sans serif; monospace only in the footer if needed. Use large readable Russian typography.

Hierarchy:
1. Small eyebrow: `INSIDE OF THE DAY · AGENT OPERATIONS`
2. Exact large headline above.
3. One short deck: `В длинной сессии уже есть дорогой опыт: поиск, ошибки, противоречия и исправления.`
4. One central, obvious mechanism panel with this heading: `В конце сессии запустите /improve`
5. Inside that panel, three numbered statements only:
   `01 Находит повторяющиеся паттерны и ошибки`
   `02 Превращает длинные команды в скрипты`
   `03 Сохраняет уроки в правила, память и skills`
6. A large, clear CTA right below: `Полный универсальный промпт - в тексте поста`
7. Small proof line: `Проверено в Claude Code.`
8. Final takeaway: `Не храните всю переписку. Сохраните улучшение системы.`
9. Footer: `github.com/mcplafed/inside-of-the-day` left and `@vitalylobachev` right.

The first glance must explain: long sessions contain learning; /improve extracts it into a better system. Avoid slogans without context, equal card grids, fake dashboards, tiny body text, and excessive design elements. Use one strong central mechanism, not a three-card generic process.
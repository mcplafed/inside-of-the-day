Create a completely new, self-contained English LinkedIn infographic as `infographic.html` in this directory. Do not access files outside this directory and do not reuse any previous visual.

Canvas: 1080x1350 logical pixels. It must be understandable on a 400x500 LinkedIn mobile feed. No external assets, no background grid, no gradients, no tiny text, no fake metrics, no generic equal-weight card grid.

Message to communicate at a glance:

Headline: `Don't bury a long agent session.`

The explanation must be obvious: a long session contains expensive operational learning (searches, mistakes, contradictions, fixes). At the end of it, `/improve` turns that learning into a better working system.

Use this factual content:
- `/improve` finds recurring patterns and mistakes.
- It turns fragile or long shell commands into tested scripts.
- It saves only durable lessons in memory, scoped rules, and skills.
- It must not retain secrets, raw logs, or temporary state.
- Full universal prompt is in the post caption.
- This approach was tested in Claude Code.

The visual needs one unmistakable central mechanism rather than a list. Make the transition visually clear: `LONG SESSION` -> `/improve` -> `BETTER NEXT SESSION`. Add concise explanatory copy so the viewer knows what each stage means. Make `FULL PROMPT IN THE POST` a large, direct but secondary CTA. Use a warm editorial background with dark slate ink and a restrained terracotta accent; the art direction is otherwise yours. Include `@vitalylobachev` in the footer.
